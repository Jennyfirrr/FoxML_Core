# FoxML Core Requirements
# For pip-only installation (not recommended - use environment.yml with conda)
# Install: pip install -r requirements.txt
#
# NOTE: Broker and execution adapters are NOT included in this file.
# FoxML Core is broker-agnostic; production execution is handled via internal/commercial adapters.

# Core Scientific Computing
numpy>=2.0.0,<3.0.0  # Tested with 2.2.6
pandas>=2.0.0,<3.0.0  # Tested with 2.3.3
scipy>=1.10.0,<2.0.0  # Tested with 1.15.3

# Data Handling & Storage
pyarrow>=20.0.0  # Tested with 22.0.0
fastparquet>=2024.0.0  # Tested with 2024.11.0
h5py>=3.15.0  # Tested with 3.15.1
tables>=3.8.0

# Machine Learning
scikit-learn>=1.7.0  # Tested with 1.7.2
joblib>=1.5.0  # Tested with 1.5.2
# XGBoost must be built from source with CUDA for GPU support
# See scripts/build_xgboost_cuda.sh
# Tested with 3.2.0-dev (built from source)

# Deep Learning (optional)
torch>=2.0.0
torchvision>=0.15.0
tensorflow>=2.19.0  # Tested with 2.19.1 (CUDA 12.9)
keras>=3.12.0  # Tested with 3.12.0

# Statistical & Time Series
statsmodels>=0.14.0
TA-Lib>=0.4.0

# Visualization
matplotlib>=3.10.0  # Tested with 3.10.7
seaborn>=0.12.0
plotly>=6.0.0  # Tested with 6.4.0

# Configuration & Utilities
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Progress & Logging
tqdm>=4.65.0
colorlog>=6.7.0

# Financial Data
# NOTE: Market data libraries are NOT included. Production customers are expected to plug in their own data feeds.
# FoxML Core is a pipeline infrastructure, not a data provider.

# API & Networking
requests>=2.31.0
aiohttp>=3.8.0
websockets>=11.0

# Broker and execution adapters are NOT included in this file.
# FoxML Core is broker-agnostic; production execution is handled via internal/commercial adapters.
# Trading integration modules have been removed from the core repository.

# ML Extras
ngboost>=0.4.0
optuna>=3.2.0
shap>=0.42.0

# Time Series
arch>=6.2.0

# Utilities
python-dateutil>=2.8.2
pytz>=2023.3
ntfy>=2.7.0

# Testing & Quality
pytest>=7.3.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.0.270

# Database
sqlalchemy>=2.0.0

# Data Processing
polars>=1.35.0  # Tested with 1.35.2 (for efficient cross-sectional processing)

# System Utilities
psutil>=7.0.0  # Tested with 7.0.0

# NOTE: LightGBM NOT included - must be built from source with CUDA
# After installing these requirements, build LightGBM:
#   bash scripts/build_lightgbm_cuda.sh
#
# NOTE: XGBoost NOT included - must be built from source with CUDA for GPU support
# After installing these requirements, build XGBoost:
#   bash scripts/build_xgboost_cuda.sh
#
# For CPU-only versions (not recommended, no GPU support):
#   pip install lightgbm xgboost

